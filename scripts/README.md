# Scripts subfolder

To run scripts.

## 📁 Structure

```text
├── analysis/               # Specific simulations to treat problems
├── plotting/               # Produce plots or figures from raw data
├── slurm/                  # Slurm to run on cluster
└── main.py                 # Main file to run a QAOA pipeline
```